[project]
name = "alembic-test-app"
version = "0.1.0"
description = "Test application for Alembic Deploy Action"
requires-python = ">=3.11"
dependencies = [
    "alembic>=1.13.0",
    "sqlalchemy>=2.0.0",
]
